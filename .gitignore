node_modules/
dist/
*.tsbuildinfo

# HTTP 응답 캐시와 증분 수집 결과 — 재생성 가능하므로 커밋하지 않는다.
data/cache/

# data/corpus/korean-words.tsv 는 더 이상 무시하지 않는다 — 우리말샘 온라인
# 수집 결과를 scripts/exportKoreanDictCorpus.mjs 가 이 경로로 내보내고,
# dictionary.yml 의 refresh 잡이 매주 자동으로 커밋한다(직접 손으로 고치지
# 말 것 — 다음 자동 갱신 때 덮어써진다).

# 빌드 산출물. 배포 파이프라인에서 만들거나 릴리스로 붙인다.
# 예시 결과물은 examples/ 에 커밋한다.
data/dist/
